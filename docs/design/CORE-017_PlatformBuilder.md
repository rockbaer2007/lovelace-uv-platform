# CORE-017 – PlatformBuilder

## Purpose
Provide the single public entry point for creating a platform instance.

## Goals
- Stable public API
- Fluent configuration
- Hide internal implementation
- Builder owns configuration, not runtime
