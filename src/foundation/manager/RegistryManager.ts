import { Manager } from "./Manager";

/**
 * Base interface for managers that use a registry.
 *
 * Visibility: Internal
 * Stability: Experimental
 */
export interface RegistryManager extends Manager {
}
