import { PlatformServices } from "../../core/PlatformServices";import { Registry } from "../../registry";import { Provider } from "../Provider";import { ProviderManager } from "../ProviderManager";import { PlatformProviderContext } from "./PlatformProviderContext";export class DefaultProviderManager implements ProviderManager{private readonly providers=new Registry<Provider<unknown>>(p=>p.metadata.id);private readonly context:PlatformProviderContext;constructor(s:PlatformServices){this.context=new PlatformProviderContext(s.registry,s.eventBus);}register(p:Provider<unknown>){this.providers.register(p);}unregister(id:string){return this.providers.unregister(id);}get(id:string){return this.providers.get(id);}getAll(){return this.providers.getAll();}initialize(){}start(){}stop(){}dispose(){}}
